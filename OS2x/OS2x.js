import * as HTMLS from './HTMLS/main.js'
import * as Terminal from './util/Terminal.js'
import * as Time from './util/Time.js'
import * as uMath from './util/uMath.js'
import * as uAnimate from './HTMLS/uAnimate.js'

export const Graphics = {
    HTMLS: HTMLS,
    uAnimate: uAnimate
}

export const Util = {
    Terminal: Terminal,
    Time: Time,
    uMath: uMath
}